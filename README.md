# Final Alert Guard Admin

This project contains the **Flutter Web UI** for the Final Alert Guard Admin panel. It provides an intuitive and responsive interface for managing alerts, users, and security-related features.

## Features
- Responsive Flutter Web UI
- Dashboard with key insights
- User management
- Modern UI with Material Design

## Installation
1. Clone the repository:
   ```sh
   git clone https://github.com/Ali-Usam4/Final-Alert-Guard-Admin.git
   ```
2. Navigate to the project directory:
   ```sh
   cd Final-Alert-Guard-Admin
   ```
3. Install dependencies:
   ```sh
   flutter pub get
   ```
4. Run the app:
   ```sh
   flutter run -d chrome
   ```

## Technologies Used
- **Flutter Web** for UI development
- **Provider/Cubit/Bloc** (if applicable) for state management
- **Firebase** (if applicable) for backend integration

